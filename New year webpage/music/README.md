<!-- Hướng dẫn: Đặt file nhạc Tết ở đây

- Đặt file MP3 bạn muốn phát tên là `tet_song.mp3`.
- Nếu muốn dùng tên file khác, chỉnh thuộc tính `src` trong `index.html` (thẻ <audio id="bg-music">).
- Trình duyệt chặn autoplay; nhấp vào nút ở góc phải dưới để bật/tắt nhạc. -->
